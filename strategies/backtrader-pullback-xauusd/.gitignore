# Generated / large data — keep real feeds and synthetic samples out of git.
data/*.csv
# Trade reports written at runtime (relative to the working directory).
temp_reports/
# Regime/time-specific tuning output — regenerate with retune_atr.py.
tuned_params.json
